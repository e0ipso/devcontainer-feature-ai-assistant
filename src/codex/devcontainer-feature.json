{
  "id": "codex",
  "version": "1.2.0",
  "name": "Codex CLI",
  "description": "Installs the Codex CLI into the dev container.",
  "options": {
    "updateOnPostStart": {
      "type": "boolean",
      "default": true,
      "description": "Run 'npm install -g @openai/codex' during the dev container postStart phase to update to the latest version."
    },
    "seedConfig": {
      "type": "boolean",
      "default": true,
      "description": "Seed ~/.codex/config.toml from a host mount (~/.cred-seed/codex/config.toml) or the image-baked default on first start only."
    }
  },
  "containerEnv": {
    "PATH": "/usr/local/share/npm-global/bin:$PATH"
  },
  "postStartCommand": "d=/usr/local/share/devcontainer-feature-ai-assistant/codex; for s in update.sh seed-config.sh; do f=\"$d/$s\"; if [ -x \"$f\" ]; then \"$f\"; fi; done",
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils",
    "ghcr.io/devcontainers/features/node",
    "ghcr.io/devcontainers/features/github-cli"
  ]
}
